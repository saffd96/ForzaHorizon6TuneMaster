<UserControl x:Class="Forza_Horizon_6_Tune_Master.Views.TransmissionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:v="clr-namespace:Forza_Horizon_6_Tune_Master.Views"
             xmlns:l="clr-namespace:Forza_Horizon_6_Tune_Master.Services">
    <StackPanel>
        <TextBlock Text="{l:Loc SectionTransmissionClutch}" Style="{StaticResource PartGroupHeader}" Margin="0,0,0,4"/>
        <v:PartRow Label="{l:Loc Part_Transmission}" Options="{Binding Transmissions}" Selected="{Binding SelectedTransmission}"/>
        <v:PartRow Label="{l:Loc Part_Clutch}"       Options="{Binding Clutches}"      Selected="{Binding SelectedClutch}"/>

        <TextBlock Text="{l:Loc SectionDrivelineDiff}" Style="{StaticResource PartGroupHeader}"/>
        <v:PartRow Label="{l:Loc Part_Driveline}"    Options="{Binding Drivelines}"    Selected="{Binding SelectedDriveline}"/>
        <v:PartRow Label="{l:Loc Part_Differential}" Options="{Binding Differentials}" Selected="{Binding SelectedDifferential}"/>
    </StackPanel>
</UserControl>
