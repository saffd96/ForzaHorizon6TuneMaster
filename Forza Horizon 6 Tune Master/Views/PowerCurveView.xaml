<UserControl x:Class="Forza_Horizon_6_Tune_Master.Views.PowerCurveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:l="clr-namespace:Forza_Horizon_6_Tune_Master.Services"
             Background="{StaticResource CardBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,8,0,0">
            <Canvas x:Name="ChartCanvas" ClipToBounds="True"/>
        </Grid>
        <WrapPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,4,0,4">
            <Rectangle Width="16" Height="3" Fill="#1ABCFE" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <TextBlock Text="{l:Loc CurveLegendTorque}" Foreground="{StaticResource TextMutedBrush}"
                       FontSize="10" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <Rectangle Width="16" Height="3" Fill="#FF5E0E" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <TextBlock Text="{l:Loc CurveLegendPower}" Foreground="{StaticResource TextMutedBrush}"
                       FontSize="10" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <TextBlock x:Name="PeakValues" Foreground="{StaticResource TextDimBrush}"
                       FontSize="10" VerticalAlignment="Center"/>
        </WrapPanel>
    </Grid>
</UserControl>
